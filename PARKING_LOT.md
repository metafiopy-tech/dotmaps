# PARKING LOT — threads from the founding conversation not yet in any spec

*Nothing here is in scope for any current session. This file exists so ideas survive without depending on anyone's memory. Each item names where it would eventually land.*

## 1. Iterative compile approval ("yes / no-with-a-point")
The compile dialogue upgraded from one-shot board approval to an iterative loop: generate board → machine pre-checks (DAG valid, verifiers runnable, attack rubric) → show → human yes or no-with-a-point → regenerate → lock manifest on yes. Round cap ~6; past that, the failure is intent-articulation, not candidates — end session, write the disagreement as words. Approvals archived as consolidation episodes (future templates). → *Lands in product spec §4.4 when the product path resumes.*

## 2. Outcome-audition mode
For stageable tasks: describe outcome → compile candidate maps → run fully in $0 staging → present N CERTIFIED finished products → human picks by outcome → winning map locks. Humans select over outcomes, not processes; every candidate reaching human eyes is already all-green (dots below the waterline, taste above it). Requires meaningfully-diverse candidates (MAP-Elites/DPP per the audit) and a map-diff view. Un-stageable tasks (send/post/publish) keep board-approval mode. → *Product spec v0.2 intake; do not build before a paying/serious stranger exists.*

## 3. Marketplace + certification gate (the Dana arc)
End-state: others submit maps; admission = passing the certification pipeline (attack + probe + ladder floor); replay GIF is the listing; stranger-submitted verifiers run in Firecracker/gVisor per the audit. Two-sided cold-start reality: seed with first-party maps first; gate opens only after strangers pay/engage. → *Post-v0.1, criteria in product spec §7 risk table.*

## 4. Certificate as settlement condition (the Kite thread)
Agentic payment rails (Kite et al.) solved identity/authorization/constraint/settlement and have an open socket at proof-of-work-done; a signed all-green certificate is shaped like that socket ("escrow releases on green manifest"). H4 tests the property (portability/independent re-verification); everything beyond H4 — chains, contracts, attestation of the sovereign runner, TEEs — stays out until the stamp exists and a partner demands it. → *One line in product spec §6 open questions; strategic, not build.*

## 5. Auto-grounding loop for prose/answers (Dot Maps for text)
Decompose answer → atomic claims → partition checkable (V) vs fog (F) → ground every V-claim with real tools → revise → RE-VERIFY FULL claim set → terminate on coverage + zero-change pass, never on confidence. Independent decomposer beats self-decomposition (claim-gerrymandering risk); iteration cap ~3 (over-correction degrades good answers); handoff artifact = green manifest + declared fog, not a confidence score. → *Possible future map genre ("research/answer maps"); also just a personal workflow.*

## 6. The model ladder as difficulty-window instrument (post-pilot refinement)
Stage 0/0b taught: the instrument must be MATCHED to the task (capability just below difficulty — IRT rediscovered empirically). Before any H1 revival: run the 5-run WINDOW ASSAY (candidate rung × anchor map, pass rate must land ~20–80%) as a pre-check, then register. The ladder floor = lower edge of the difficulty window; "empty window" is itself a reportable map property. → *Amendment to instrument spec if/when pilot 3 registers.*

## 7. Grounded-convergence research program (separate project)
`grounded_convergence_spec.md` (E1–E5: two-store memory consolidation, cadence, mastery-vs-decay proxy signals, tether, oracle-strength threshold) belongs to the Belief Engine, not pac/. Superseded in priority by the instrument work; not superseded in content. E4's frozen-vs-tethered logic already lives on as map-3/heartbeat thinking. → *Keep the file with the Belief Engine repo.*

## 8. The crew oracle
The actual goal named out loud: working with people like Joe. Its verifier is not revenue and not all-green — it is a serious stranger engaging with the artifacts. Concrete dots when ready: pilot_report findings written up honestly (the 3B fabricating victory 29× while the board stayed red is the opening anecdote; "the board stayed red" is the sentence), corpus generator + one certified map + replay public on metafiopy-tech. Artifacts recruit better half-finished-and-honest than perfect-and-late. → *Not a build item. A contact item. The one dot no synthesis phase can eat.*
