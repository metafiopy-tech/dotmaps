# Numbers

Not a claim — a live read of her own record, computed fresh every time
this page loads, the same way every other number in this product is
computed: never a stored label, always re-derived from the actual files.

- **{{skills_total}} things learned**, of which **{{skills_certified}}**
  have cleared the real statistical-confidence check and count as
  certified — trusted enough to answer for free.
- **{{free_executions}} replies answered for nothing** so far — every
  one of them a case where a certified fact stood in for a model call.
- **{{model_calls_total}} times** she's gone and done real work instead,
  because nothing certified could answer yet.
- **{{decision_chain_length}} decisions** recorded on her append-only
  record, unbroken start to finish — a single edited or reordered line
  anywhere in that history would be caught, mechanically, on the next
  read.
- **{{chat_chain_length}} messages** in her conversation record, same
  guarantee: append-only, checked whole every time it's read.

Every one of these numbers is one query away from the file it came from
— nothing here is summarized by hand, and nothing here is asked to be
believed.
