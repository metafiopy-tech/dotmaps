# Fog — what the smoke map does NOT decide

This is a self-test map for the harness. It decides only two mechanical facts:
a file exists, and it contains a token. It certifies nothing about the real
world. There are no human-approval items — it is intentionally trivial.
