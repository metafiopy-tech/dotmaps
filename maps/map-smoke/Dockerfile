# Pinned verifier environment for the smoke map (the portability moat, spec §4.1).
# Verifiers run here with the workspace bind-mounted READ-ONLY.
FROM python:3.11-slim
WORKDIR /map
# smoke verifiers are stdlib-only; nothing to install.
