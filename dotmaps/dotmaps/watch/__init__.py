"""WATCH — point-and-watch (W1-W3, branch queen-watch off queen-v1).

"The keeper points her at a REAL thing and WATCHES verification happen in
the browser." A watch target compiles into a HEALTH MAP: a set of dots
whose checks are HTTP/DOM facts (page responds, title non-empty, a form
answers GET, a declared asset loads) — mechanical, no model anywhere in
this loop. Reuses the exact primitives the rest of the queen already
trusts: `grow.banking.run_steps`/`evaluate` execute each check (the same
walk a bank skill's `method.steps` takes), `bank.certify.wilson` computes
the same interval every other certificate uses, and every event is a trip
(`queen.trips`) — no parallel state file. A dot that verifies clean across
CERT_N consecutive checks earns a real skill card under skills/watch/
(kept out of skills/'s top-level glob so bank/certify.py's generic
reverify — which assumes filesystem-workspace break-copy semantics — never
touches a live-URL card; see queen/assure.py's dedicated watch-oracle row
for HOW a watch card gets reverified instead).
"""
