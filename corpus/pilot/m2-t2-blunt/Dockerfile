# Pinned verifier environment for map 2 (the portability moat). The migration
# dots are stdlib-only; only m05 in HTTP mode needs network (declared in map.yaml).
FROM python:3.11-slim-bookworm
WORKDIR /map
